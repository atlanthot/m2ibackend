<!doctype html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<title>forum</title>
	<link href="{{ asset('./css/main.css') }}" type="text/css" rel="stylesheet" />
</head>
<body>

	<div align="right"><a href="{{ path('m2iprojet_admin_route')}}" title="retour">
	<img src="{{ asset('./images/retour.jpg') }}" width="40px" height="40px" style="border-radius:20px;border: black solid 1px"></a></div> 
	{% for current_testimony in testimonys_list %}
		<li>
			<label style="color:gray"><b>{{ current_testimony['firstname'] }} {{ current_testimony['lastname'] }}</b></label>
			<label style="color:gray">( {{ current_testimony['creationDate']|date("d/m/Y h:m") }} )</label>
			<br/>
			<div>
				{{ current_testimony['msg'] }}
			</div>
			<br/>
		</li> 
		<br/>
	{% endfor %}




	{{ form_start(form) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}	
</body>
</html>